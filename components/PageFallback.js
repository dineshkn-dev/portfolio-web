export default function PageFallback() {
    return (
        <div className="site-module min-h-[50vh] flex items-center justify-center" aria-hidden="true">
            <p className="site-eyebrow animate-pulse">Loading page...</p>
        </div>
    );
}
