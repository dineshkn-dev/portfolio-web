## Summary

<!-- What changed and why (1–3 sentences) -->

## Type of change

- [ ] Content / copy (`lib/site-content.js`)
- [ ] UI / shell
- [ ] New route or module
- [ ] Performance
- [ ] Dependencies
- [ ] Agent docs only

## Verification

CI runs `npm run verify` + Lighthouse automatically on this PR.

- [ ] `npm run verify` passed locally
- [ ] Synced files committed if routes/scripts changed (`llms.txt`, `AGENTS.md`, `docs/generated/manifest.json`)
- [ ] Manual smoke: `/`, one module, theme toggle, mobile width (if UI)

## Agent docs

- [ ] Updated `AGENTS.md` / `docs/` if commands, routes, or architecture changed

## Screenshots / metrics

<!-- Optional -->
